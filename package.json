{
  "name": "radish-ui",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm --filter @radish-ui/core build",
    "dev": "pnpm --filter @radish-ui/demo dev",
    "preview": "pnpm --filter @radish-ui/demo preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
